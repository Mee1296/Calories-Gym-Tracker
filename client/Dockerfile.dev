# syntax=docker/dockerfile:1
# Development image: Next dev server, source bind-mounted by compose.
FROM node:20-alpine
ENV NODE_ENV=development \
    NEXT_TELEMETRY_DISABLED=1
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

EXPOSE 3000
CMD ["npm", "run", "dev"]
